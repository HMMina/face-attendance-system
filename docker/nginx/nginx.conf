# Nginx config cho local network
worker_processes 1;
events { worker_connections 1024; }
http {
    server {
        listen 80;
        location /api/ {
            proxy_pass http://backend:8000/api/;
        }
        location / {
            proxy_pass http://admin:3000/;
        }
    }
}
